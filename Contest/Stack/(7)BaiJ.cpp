#include <bits/stdc++.h>
#define Faster() ios_base::sync_with_stdio(false); cin.tie(NULL);cout.tie(NULL);
#define C 1000000007
using namespace std;
void Solve()
{
	int n;cin>>n;
    long long a[n+5];
    for(int i=0;i<n;i++) cin>>a[i];
    sort(a,a+n);
    //Ý tưởng :số lớn nhất đầu tiên bên phải
    cout<<res;
}
int main()
{
	Faster();
	Solve();
	return 0;
}